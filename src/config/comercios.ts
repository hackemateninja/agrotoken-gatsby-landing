import { ISeoConfig } from '@def/ISeoConfig';

const ComerciosSeo: ISeoConfig = {
	title: 'Agrotoken Comercios adheridos',
	description: 'Comercios que aceptan agrotokens como métodos de pago.',
	meta: [],
};

export default ComerciosSeo;
